#Starting a cluster
#Choose a clovr configuration file
#such as clovr-IGSnimbus.ini

#Check the following settings
IMAGE=clovr.9-04.x86-64.beta.img
#exists in the cloud

KEY=mykey2
#has been added with ec2-add-keypair

CLUSTER_PRIVATE_KEY=/home/sangiuoli/.ssh/devel1.pem
#is a valid SSH key that you can access

EC2_HOME=/usr/local/projects/angiuoli/ec2-api-tools-1.3-24159
#is a compatible version of the EC2 API for Nimbus

EC2_PRIVATE_KEY=~/.globus/userkey-ec2.fixed.pem
EC2_CERT=~/.globus/usercert-ec2.pem
EC2_JVM_ARGS=-Djavax.net.ssl.trustStore=/home/sangiuoli/.globus/jssecacerts.igs
JAVA_HOME=/usr/lib/jvm/jre-1.6.0-sun/
#are all set properly 

#Invoke start_cluster
cd cli
./start_cluster.pl --conf ./clovr-IGSnimbus.ini

#start_cluster will return the ip address of the master node 
#ergatis is then accessible through 

http://<MASTER_IP>//ergatis/cgi/index.cgi